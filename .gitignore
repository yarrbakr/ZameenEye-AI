node_modules/
.env
dist/
__pycache__/
venv/
.venv/
*.pyc

# planning docs (may contain personal info — e.g. the uncle's phone number)
documents/

# generated audio replies (written to system temp at runtime, but just in case)
advisory_*.mp3

# CV lane: model weights auto-downloaded at runtime + segmentation outputs
*.pt
cv_out/

# YOLO training: ultralytics run outputs + the runtime-generated dataset yaml
runs/
python/runs/
data.local.yaml
python/data.local.yaml